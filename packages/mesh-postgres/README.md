# @corelay/mesh-postgres

Postgres durability for Corelay Mesh: durable workflow store and `Inbox` implementation.

**Status: Week 1 — scaffolding. No runtime code yet.**

Requires a Postgres database. Apply `sql/001-init.sql` manually for now; migrations come later.
