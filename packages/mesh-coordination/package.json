{
  "name": "@corelay/mesh-coordination",
  "version": "0.0.1",
  "description": "Named coordination primitives for Corelay Mesh: Critic, Hierarchy, Pipeline, Human-in-the-loop.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@corelay/mesh-core": "0.0.1",
    "@corelay/mesh-observe": "0.0.1"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.8"
  },
  "publishConfig": {
    "access": "public"
  }
}
